// FirebaseMessaging.instance.getToken().then((token) {
//   print("FCM Token: $token");
//   // Kirim token ke server
//   sendTokenToServer(token);
// });

// void sendTokenToServer(String token) {
//   // Implementasikan logika untuk mengirim token ke server
// }
